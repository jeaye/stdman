.TH std::list::~list 3 "2024.06.10" "http://cppreference.com" "C++ Standard Libary"
.SH NAME
std::list::~list \- std::list::~list

.SH Synopsis
   ~list();

   Destructs the list. The destructors of the elements are called and the used storage
   is deallocated. Note, that if the elements are pointers, the pointed-to objects are
   not destroyed.

.SH Complexity

   Linear in the size of the list.
